# Railway: Root Directory = report-service → acest fișier forțează build Docker (Puppeteer + Chromium).
# https://docs.railway.com/reference/config-as-code
[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"
